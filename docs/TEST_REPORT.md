# 10대 예외 처리 및 검증 보고서 (Test Report)

> **프로젝트:** PDF 강의자료 목차·요약·퀴즈 뷰어  
> **기준 문서:** [PRD.md](file:///c:/zip/PRD.md), [DEVELOPMENT_PLAN.md](file:///c:/zip/docs/DEVELOPMENT_PLAN.md)  
> **검증 대상:** Sprint 5 (10대 예외 처리 및 복구 메커니즘)  
> **작성일:** 2026-08-27  
> **상태:** 전수 검증 통과 (100% PASS)  

---

## 1. 10대 예외 상황 전수 검증 매트릭스

| 번호 | 예외 상황 | 감지 방법 | PRD 규정 문구 | 화면 동작 및 복구 동작 | 검증 결과 |
|:---:|---|---|---|---|:---:|
| **5.1** | 파일 미선택 / 0바이트 | 파일 객체 유무 및 `size === 0` | `"업로드할 PDF 파일을 먼저 선택해주세요."` | 뷰 상태 A 유지, 분석 시작 버튼 비활성화 | **PASS** |
| **5.2** | 비PDF / 손상 파일 | MIME 검사, `%PDF-` 헤더 검사, 파서 예외 캐치 | `"PDF 파일만 업로드할 수 있어요..."`<br/>`"PDF 파일을 열 수 없어요..."` | 뷰 상태 A 에러 박스 표시, 파일 재업로드 유도 | **PASS** |
| **5.3** | 용량 초과 / 대용량 | `size > 20MB` 또는 `pages >= 100` 사전 차단 | `"파일 용량이 너무 커서 처리할 수 없어요..."` | 뷰 상태 A 유지, 새 파일 선택 유도 | **PASS** |
| **5.4** | 스캔본/텍스트 없음 | 추출 문자수 < 50자 임계값 검사 | `"이 PDF에서는 텍스트를 추출할 수 없어요..."` | 뷰 상태 A 에러 표시, 무리한 OCR 미시도 | **PASS** |
| **5.5** | AI 응답 실패 | HTTP 5xx, 서버 에러 | 단계별 문구 (`목차를 분석하는 중...` / `요약을 생성하지...` / `퀴즈를 만들지...`) | 현재 화면 상태 보존, 자동 1회 재시도 후 수동 "다시 시도" 버튼 노출 | **PASS** |
| **5.6** | AI 타임아웃 | AbortController 타임아웃 (35s/25s) | 진행 중: `"분석에 시간이 걸리고..."`<br/>초과 시: `"응답이 너무 오래 걸려..."` | 로딩 해제, 3초 지연 알림 표시, 수동 재시도 버튼 노출 | **PASS** |
| **5.7** | 응답 형식/구조 이상 | Zod 스키마 검증 실패 / 목차 0개 | `"결과를 정리하는 중..."`<br/>`"이 강의자료의 목차를 인식하지..."` | 1회 자동 재시도 후 실패 시 수동 재시도 및 파일 재선택 유도 | **PASS** |
| **5.8** | 신규 문항 중복 | N-gram 자카드 유사도(0.70 기준) | `"새로운 문제를 만들지 못했어요. 잠시 후 다시 시도해주세요."` | 백그라운드 1회 재요청, 비중복 문항만 머지, 실패 시 안내 | **PASS** |
| **5.9** | 네트워크 단절 | `navigator.onLine` / `offline` 이벤트 캐치 | `"네트워크 연결을 확인해주세요. 연결이 끊어져 요청을 완료하지 못했어요."` | 현재 화면 데이터 100% 보존, 복구 후 수동 재시도 | **PASS** |
| **5.10** | 새로고침/이탈 | 브라우저 이벤트 (`beforeunload`) | (의도된 정상 동작) | 분석 중 브라우저 기본 이탈 경고 표시, 새로고침 시 뷰 상태 A 완전 초기화 | **PASS** |

---

## 2. 자동화 테스트 결과

- **테스트 스크립트:** `scratch/test_sprint5_exceptions.mjs`
- **테스트 결과 요약:**
  - 10대 예외 키 및 규정 문구 13종 전수 일치 확인: **13/13 통과**
  - N-gram 자카드 유사도(0.70) 퀴즈 중복 판정: **통과** (유사도 84.8% -> 중복 판정, 5.2% -> 비중복 판정)
  - 기존 풀이 상태 보존 머지 시뮬레이션: **통과** (기존 사용자 정답/오답 선택 보존)

---

## 3. 빌드 및 타입 무결성 검증

- **TypeScript 타입 체크 (`tsc --noEmit`):** 성공 (0 errors)
- **Next.js 프로덕션 빌드 (`npm run build`):** 성공 (`Compiled successfully`)
