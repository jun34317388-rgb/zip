# PRD: PDF 강의자료 목차·요약·퀴즈 뷰어

## 문서 정보
- 작성 목적: AI 코딩 도구가 그대로 읽고 구현할 수 있는 실행 스펙
- 범위 제약: 단일 화면(단일 페이지) 구성, 핵심 기능 1개, 로그인/회원가입/결제/DB 없음
- 기술 스택: 미지정 (구현 도구가 자유롭게 선택). 이 문서는 "무엇을 만들 것인지"만 정의한다.
- 데이터 보관: 브라우저 세션(메모리) 한정, 새로고침 시 초기화되어도 무방

---

## 1) 목표와 성공조건

### 목표
사용자가 텍스트 기반 PDF 강의자료를 업로드하면, 목차 단위로 구조를 자동 파악하고, 각 목차별로 "핵심만 압축한 요약"과 "이해도를 확인하는 퀴즈"를 즉시 확인할 수 있게 한다. 전체 문서를 처음부터 끝까지 읽지 않아도 어디에 무엇이 있는지, 그리고 자신이 그것을 이해했는지를 스스로 점검할 수 있게 하는 것이 이 앱의 유일한 기능이다.

### 성공조건 (Success Criteria)
1. 5시간 내 구현 가능한 범위에서, "PDF 업로드 → 목차/요약/퀴즈 출력"까지 끊김 없이 동작하는 데모 가능한 프로토타입이 완성된다.
2. 텍스트 기반 강의자료 PDF(20~40페이지) 기준으로 업로드 후 1분 이내에 목차 구조화 결과가 화면에 표시된다.
3. 실제 강의자료 3종으로 테스트했을 때, 자동 인식된 목차(챕터/섹션 제목)가 원문 목차와 육안으로 크게 어긋나지 않는다.
4. 생성된 요약에 원문에 없는 내용(사실 왜곡, 근거 없는 추가 정보)이 포함되지 않는다(육안 검수 기준).
5. 요약만 읽어도 해당 목차의 핵심 개념과 정의를 제3자에게 설명할 수 있는 수준의 밀도를 갖는다(불릿 5~10개 내외, 한 줄 요약 수준 금지).
6. 생성된 퀴즈는 별도 자료 없이 해당 목차의 요약만으로 풀 수 있는 난이도와 범위를 유지한다.
7. 목차 카드 클릭 → 상세 화면 진입 → 요약/퀴즈 선택 → 목록으로 복귀까지의 화면 전환이 로딩 지연이나 깨짐 없이 동작한다.
8. 퀴즈를 다 푼 뒤 "문제 더 풀기"를 누르면, 기존에 출제된 문항과 중복되지 않는 새 문항 2~3개가 정상적으로 추가 생성된다.

---

## 2) 사용자 스토리

- 자격증 시험을 준비하는 학습자로서, 나는 강의자료 PDF를 올리기만 하면 전체를 읽지 않고도 목차별로 무슨 내용이 있는지 한눈에 보고 싶다. 그래야 시험 전 남은 시간 안에 우선순위를 정해 훑어볼 수 있기 때문이다.
- 사내 교육을 듣는 직장인으로서, 나는 특정 챕터를 클릭했을 때 그 챕터의 핵심만 불릿으로 정리한 요약을 보고 싶다. 그래야 회의 전 짧은 시간에 내용을 파악할 수 있기 때문이다.
- 학습자로서, 나는 요약을 읽은 직후 같은 챕터 기반의 퀴즈를 풀어보고 싶다. 그래야 내가 실제로 이해했는지 스스로 확인할 수 있기 때문이다.
- 학습자로서, 나는 퀴즈 2~3문항을 다 풀고도 더 확인하고 싶으면 "문제 더 풀기"로 새 문항을 받고 싶다. 그래야 부족하다고 느낄 때 추가로 점검할 수 있기 때문이다.
- 학습자로서, 나는 상세 화면에서 언제든 목록으로 돌아가 다른 챕터를 선택하고 싶다. 그래야 여러 챕터를 오가며 훑어볼 수 있기 때문이다.
- 학습자로서, 나는 로그인이나 회원가입 없이 바로 PDF를 올리고 결과를 보고 싶다. 그래야 진입 장벽 없이 즉시 사용할 수 있기 때문이다.

---

## 3) 화면 명세

이 앱은 하나의 페이지(단일 화면) 안에서 뷰 상태(view state)를 전환하는 방식으로 동작한다. 별도의 페이지 이동(URL 라우팅)이 아니라, 같은 화면 안에서 아래 세 가지 뷰 상태만 존재한다.

### 공통 레이아웃
- 화면 최상단에 앱 제목(예: "강의자료 목차 요약·퀴즈") 고정 표시.
- 현재 뷰 상태에 따라 아래 영역의 내용만 바뀐다.

### 뷰 상태 A — 업로드 화면 (초기 상태)
- PDF 파일 선택/드래그 앤 드롭 영역 1개.
- "업로드 및 분석 시작" 버튼 1개 (파일 선택 전에는 비활성화).
- 분석 진행 중에는 버튼 대신 로딩 인디케이터와 "목차를 분석하고 있습니다..." 문구 표시.
- 오류 발생 시 이 영역 하단에 오류 메시지 박스 표시 (5번 항목 예외 처리 참조).

### 뷰 상태 B — 목차 목록 화면
- 분석 완료 후 자동 전환.
- 인식된 목차(챕터/섹션) 개수만큼 카드(버튼) 목록을 세로로 표시.
- 각 카드에는 목차 제목만 표시(불필요한 부가 정보 없음).
- 카드를 클릭하면 뷰 상태 C(상세 화면)로 전환되며, 어떤 목차를 선택했는지 상태로 유지.
- 화면 상단에 "새 PDF 업로드" 버튼을 두어 언제든 뷰 상태 A로 되돌아가 처음부터 다시 시작할 수 있게 한다.

### 뷰 상태 C — 목차 상세 화면
- 상단에 현재 선택된 목차 제목 표시.
- 상단에 "요약 보기" / "퀴즈 풀기" 두 개의 토글 버튼 표시. 기본 선택 상태는 없음(둘 다 미선택) 또는 "요약 보기"가 기본 선택.
- 선택한 토글에 해당하는 콘텐츠만 화면에 표시하고, 다른 콘텐츠는 숨긴다.
  - "요약 보기" 선택 시: 해당 목차의 요약 불릿 목록(5~10개 내외) 표시.
  - "퀴즈 풀기" 선택 시: 해당 목차 기반 객관식 문항 2~3개 표시. 각 문항은 보기 선택 후 정답 확인이 가능해야 한다(정답/오답 표시).
- 퀴즈를 모두 풀면(모든 문항에 답을 선택하면) 화면 하단에 "문제 더 풀기" 버튼이 나타난다.
  - 클릭 시 같은 목차 내용을 기반으로 기존 문항과 겹치지 않는 새 문항 2~3개가 추가로 생성되어 기존 문항 아래에 이어 붙는다.
- 화면 상단 또는 하단에 "목록으로 돌아가기" 버튼을 두어 뷰 상태 B로 전환한다. 이때 해당 목차에서 진행한 요약/퀴즈 상태는 유지하지 않아도 된다.
- 콘텐츠 생성 중(요약 생성, 퀴즈 생성, 추가 문항 생성)에는 해당 영역에 로딩 인디케이터를 표시하고 버튼은 중복 클릭되지 않도록 비활성화한다.

---

## 4) 기능 명세

이 앱의 핵심 기능은 하나다: "PDF 강의자료를 목차 단위로 구조화하고, 목차별 요약과 퀴즈를 생성해 보여준다." 아래는 이 단일 기능을 구성하는 처리 단계다.

### 4.1 PDF 업로드 및 텍스트 추출
- 사용자가 PDF 파일 1개를 선택하면 클라이언트 또는 서버에서 텍스트 레이어를 추출한다.
- 추출된 텍스트가 일정 길이 이상 확보되어야 다음 단계(목차 구조화)로 진행한다.
- 텍스트 추출이 실패하거나 결과가 비어 있으면 목차 구조화 단계로 넘어가지 않고 오류를 표시한다(5번 항목 참조).

### 4.2 목차(챕터/섹션) 자동 구조화
- 추출된 텍스트 전체를 LLM에 전달하여, 강의자료의 목차 구조(챕터/섹션 제목과 각 목차에 해당하는 본문 범위)를 추론한다.
- 결과는 "목차 제목 + 해당 목차의 원문 텍스트" 목록 형태로 구성되어야 한다. 이후 요약/퀴즈 생성은 이 원문 텍스트 범위 내에서만 이루어진다(원문에 없는 내용을 지어내지 않도록 하기 위함).
- 목차가 1개도 인식되지 않거나 목차 수가 비정상적으로 많은 경우(예: 목차 인식 실패로 문단마다 쪼개진 경우) 결과 이상 처리로 분류한다(5번 항목 참조).

### 4.3 요약 생성
- 사용자가 특정 목차의 "요약 보기"를 처음 선택한 시점에 해당 목차의 원문 텍스트를 LLM에 전달해 요약을 생성한다(전체 목차를 한 번에 미리 생성할 필요 없음, 선택 시점에 생성해도 됨).
- 요약은 핵심 개념, 정의, 예시 위주로 구성하며 불릿 5~10개 내외 분량을 목표로 한다.
- 요약 생성 시 프롬프트에는 "제공된 원문 범위를 벗어난 내용을 추가하지 말 것"을 명시적으로 포함한다.
- 같은 목차의 요약을 재조회할 경우 이미 생성된 결과를 세션 내에서 재사용하고 재생성하지 않는다(불필요한 재호출 방지).

### 4.4 퀴즈 생성
- 사용자가 특정 목차의 "퀴즈 풀기"를 처음 선택한 시점에 해당 목차의 원문 텍스트(또는 생성된 요약)를 기반으로 객관식 문항 2~3개를 생성한다.
- 각 문항은 보기 4개 내외와 정답 1개를 포함하며, 문항 난이도는 해당 목차 요약만으로 풀 수 있는 수준으로 제한한다.
- 사용자가 보기를 선택하면 즉시 정답 여부를 표시한다.

### 4.5 추가 문항 생성 ("문제 더 풀기")
- 현재 목차에 표시된 모든 문항에 답이 선택된 시점에 "문제 더 풀기" 버튼이 노출된다.
- 클릭 시 같은 목차의 원문 텍스트를 기반으로 새 문항 2~3개를 생성한다.
- 새 문항 생성 요청 시, 기존에 이미 생성된 문항 목록(질문 텍스트)을 함께 LLM에 전달하여 동일하거나 거의 동일한 문항이 중복 생성되지 않도록 한다.
- 새 문항은 기존 문항 아래에 이어서 추가되며, 기존 문항의 풀이 상태(선택한 답, 정답 여부 표시)는 그대로 유지된다.

### 4.6 화면 전환 및 상태 유지
- 목차 목록 → 상세 화면 → 목록으로 돌아가기 흐름은 서버 재요청 없이(또는 최소한의 재요청으로) 즉시 전환되어야 한다.
- 목차 목록, 각 목차별로 생성된 요약/퀴즈 결과는 세션이 유지되는 동안(새로고침 전까지) 메모리에 보관한다. 같은 목차를 다시 눌러도 이미 생성된 결과가 있으면 재사용한다.

---

## 5) 예외 처리

이 섹션이 이 문서에서 가장 중요하다. 모든 예외는 "① 상황 ② 감지 방법 ③ 사용자에게 보여줄 문구 ④ 시스템 동작(화면 상태) ⑤ 재시도 가능 여부" 다섯 가지 기준으로 정의한다. 사용자가 직접 언급한 5가지(입력 없음, 파일 과대, AI 응답 실패, 응답 지연, 결과 이상)를 포함해 총 10가지로 구성한다.

### 5.1 입력이 비어있는 경우 (파일 미선택 / 빈 파일)
- 상황: 사용자가 파일을 선택하지 않고 "업로드 및 분석 시작"을 누르거나, 선택한 파일의 용량이 0바이트인 경우.
- 감지 방법: 업로드 시작 요청 시 파일 객체 존재 여부와 파일 크기(> 0바이트)를 검사.
- 사용자 문구: "업로드할 PDF 파일을 먼저 선택해주세요."
- 시스템 동작: 업로드 화면(뷰 상태 A)에 머무르며 분석 단계로 진행하지 않는다. 버튼은 비활성화 상태 유지.
- 재시도: 파일을 다시 선택하면 즉시 정상 진행 가능.

### 5.2 파일이 PDF가 아니거나 확장자만 PDF인 손상 파일인 경우
- 상황: 사용자가 PDF가 아닌 파일(이미지, docx 등)을 올리거나, 확장자는 .pdf이지만 파일 구조가 손상되어 파서가 열 수 없는 경우.
- 감지 방법: 파일 MIME 타입/시그니처 확인 및 PDF 파싱 시도 시 예외 발생 여부.
- 사용자 문구: "PDF 파일만 업로드할 수 있어요. 파일 형식을 확인해주세요." (손상 파일의 경우) "PDF 파일을 열 수 없어요. 파일이 손상되지 않았는지 확인해주세요."
- 시스템 동작: 업로드 화면(뷰 상태 A)에 오류 메시지 박스를 표시하고 이전 단계로 되돌린다. 목차 구조화는 시도하지 않는다.
- 재시도: 다른 파일로 즉시 재업로드 가능.

### 5.3 파일이 너무 큰 경우
- 상황: 업로드한 PDF 파일 용량 또는 페이지 수가 처리 가능한 상한을 초과(예: 지정한 용량 한도 초과, 또는 지나치게 많은 페이지 수로 1분 이내 처리가 불가능한 수준).
- 감지 방법: 업로드 시점에 파일 크기 검사(사전 차단), 필요 시 페이지 수 파싱 후 상한 초과 여부 재확인.
- 사용자 문구: "파일 용량이 너무 커서 처리할 수 없어요. 더 작은 파일이나 일부 페이지만 포함된 PDF로 다시 시도해주세요."
- 시스템 동작: 분석을 시작하지 않고 업로드 화면(뷰 상태 A)에서 차단한다. 이미 분석이 시작된 뒤 크기 초과가 확인된 경우 즉시 중단하고 동일 문구를 표시한다.
- 재시도: 더 작은 파일로 재업로드 가능. 동일 파일로는 재시도해도 같은 결과이므로 재시도 버튼 대신 새 파일 선택을 유도한다.

### 5.4 텍스트 추출이 불가능한 경우 (스캔본/이미지 기반 PDF)
- 상황: PDF에 텍스트 레이어가 없어(이미지로만 구성된 스캔본) 텍스트 추출 결과가 비어 있거나 의미 있는 분량 이하인 경우.
- 감지 방법: 텍스트 추출 후 전체 문자 수가 최소 임계값(예: 특정 글자 수) 미만인지 확인.
- 사용자 문구: "이 PDF에서는 텍스트를 추출할 수 없어요. 스캔된 이미지 PDF는 지원하지 않으며, 텍스트가 포함된 PDF만 처리할 수 있어요."
- 시스템 동작: 목차 구조화 단계로 진행하지 않고 업로드 화면으로 되돌아가 오류를 표시한다. OCR 등 추가 처리는 시도하지 않는다.
- 재시도: 텍스트 레이어가 있는 다른 PDF로 재업로드 유도.

### 5.5 AI(LLM) 응답이 실패한 경우 (목차 구조화/요약/퀴즈 생성 공통)
- 상황: LLM 호출이 네트워크 오류, 서버 오류(5xx), 인증/한도 오류 등으로 실패하는 경우. 목차 구조화, 요약 생성, 퀴즈 생성, 추가 문항 생성 각 단계에서 동일하게 발생 가능.
- 감지 방법: API 호출 실패(에러 응답 또는 예외) 캐치.
- 사용자 문구: 단계별로 구분하여 표시. 예) "목차를 분석하는 중 문제가 발생했어요. 다시 시도해주세요." / "요약을 생성하지 못했어요. 다시 시도해주세요." / "퀴즈를 만들지 못했어요. 다시 시도해주세요."
- 시스템 동작: 실패한 단계 이전 화면 상태를 유지한다(예: 요약 생성 실패 시 상세 화면에는 그대로 머물되 요약 영역에 오류와 "다시 시도" 버튼을 표시). 이미 생성되어 있던 다른 목차/콘텐츠 데이터는 보존한다.
- 재시도: 해당 영역의 "다시 시도" 버튼으로 동일 요청을 재실행할 수 있다. 자동 재시도는 최대 1회까지만 수행하고, 그 이후에는 사용자가 직접 눌러야 재시도되도록 한다(무한 자동 재시도 방지).

### 5.6 AI 응답이 오래 걸리는 경우 (타임아웃)
- 상황: LLM 응답이 정상 범위(예: 목차 구조화는 수십 초, 요약/퀴즈 생성은 그보다 짧은 시간) 내에 오지 않고 지연되는 경우.
- 감지 방법: 각 요청에 타임아웃 시간을 설정하고, 초과 시 요청을 취소 처리.
- 사용자 문구: 진행 중에는 "분석에 시간이 걸리고 있어요. 잠시만 기다려주세요."를 일정 시간 경과 후 노출하고, 타임아웃 초과 시 "응답이 너무 오래 걸려 처리를 중단했어요. 다시 시도해주세요."로 전환.
- 시스템 동작: 타임아웃 도달 시 로딩 상태를 해제하고 요청을 중단하며, 5.5와 동일하게 재시도 버튼을 노출한다. 진행 중 화면이 멈춘 것처럼 보이지 않도록 로딩 인디케이터와 경과 안내 문구를 함께 표시한다.
- 재시도: 사용자가 직접 재시도 버튼을 눌러 재요청.

### 5.7 AI 응답 결과가 이상한 경우 (형식 오류/파싱 실패/비정상 결과)
- 상황: LLM 응답이 기대한 형식(목차 목록, 요약 불릿, 퀴즈 문항+정답 구조)에 맞지 않아 파싱에 실패하거나, 목차가 0개로 인식되거나, 퀴즈에 정답이 없거나 보기가 1개뿐인 등 구조적으로 비정상인 경우.
- 감지 방법: 응답을 파싱한 뒤 필수 필드(목차 제목 존재 여부, 요약 불릿 개수, 퀴즈 보기/정답 개수) 유효성 검사를 수행. 검사 실패 시 이상 결과로 분류.
- 사용자 문구: "결과를 정리하는 중 문제가 발생했어요. 다시 시도해주세요." (목차 자체가 비정상인 경우) "이 강의자료의 목차를 인식하지 못했어요. 다른 파일로 시도해보시거나 다시 시도해주세요."
- 시스템 동작: 유효성 검사를 통과하지 못한 결과는 화면에 표시하지 않고 즉시 재요청하거나(1회 자동 재시도) 오류 상태로 전환한다. 목차 구조화 자체가 실패한 경우 업로드 화면으로 되돌리고, 특정 목차의 요약/퀴즈만 이상한 경우 해당 영역만 오류 처리하고 다른 목차는 영향받지 않는다.
- 재시도: 자동 재시도 1회 후에도 동일하면 사용자가 수동으로 재시도하거나 다른 파일을 시도하도록 안내.

### 5.8 퀴즈 추가 생성("문제 더 풀기") 시 신규 문항이 기존 문항과 중복되는 경우
- 상황: LLM이 새 문항을 생성했지만 기존 문항과 질문 내용이 동일하거나 매우 유사한 경우.
- 감지 방법: 신규 문항 텍스트와 기존 문항 텍스트를 비교(단순 텍스트 일치 또는 높은 유사도)하여 중복 여부 판단.
- 사용자 문구: 사용자에게는 별도 오류를 노출하지 않고 백그라운드에서 처리한다. 반복 실패 시에만 "새로운 문제를 만들지 못했어요. 잠시 후 다시 시도해주세요."
- 시스템 동작: 중복이 감지되면 내부적으로 1회 재요청하여 대체 문항을 받는다. 재요청 후에도 중복이면 중복 문항은 제외하고 생성된 문항 중 중복이 아닌 것만 추가한다(0개면 5.5의 실패 처리로 전환).
- 재시도: 사용자가 "문제 더 풀기"를 다시 눌러 재시도 가능.

### 5.9 네트워크 연결이 끊기거나 불안정한 경우
- 상황: 업로드, 목차 구조화, 요약/퀴즈 생성 요청 도중 사용자의 네트워크 연결이 끊기는 경우.
- 감지 방법: 네트워크 요청 실패(연결 오류)를 캐치.
- 사용자 문구: "네트워크 연결을 확인해주세요. 연결이 끊어져 요청을 완료하지 못했어요."
- 시스템 동작: 진행 중이던 화면 상태를 그대로 유지하고(데이터 손실 없이) 재시도 버튼을 노출한다.
- 재시도: 네트워크 복구 후 재시도 버튼으로 동일 요청 재실행.

### 5.10 세션 중 브라우저 새로고침/이탈
- 상황: 사용자가 분석 도중 또는 결과 확인 도중 새로고침하거나 페이지를 벗어나는 경우.
- 감지 방법: 별도 감지 불필요(요구사항상 DB/영속 저장소가 없으므로 새로고침 시 초기화는 정상 동작).
- 사용자 문구: 없음(정상 동작으로 간주). 단, 진행 중 이탈을 방지하고 싶다면 브라우저 기본 이탈 확인 다이얼로그 수준까지만 허용(추가 커스텀 경고 UI는 범위 밖).
- 시스템 동작: 새로고침 시 업로드 화면(뷰 상태 A)으로 완전히 초기화된다. 이는 의도된 동작이며 결함이 아니다.
- 재시도: 해당 없음(처음부터 다시 업로드).

---

## 6) 완료 조건 (Definition of Done)

1. 업로드 화면에서 텍스트 기반 PDF를 올리면 1분 이내에 목차 목록 화면으로 전환된다.
2. 목차 목록의 각 카드를 클릭하면 상세 화면으로 전환되고, "목록으로 돌아가기"로 다시 목차 목록으로 돌아올 수 있다.
3. 상세 화면에서 "요약 보기"를 선택하면 불릿 5~10개 내외의 요약이 표시되고, "퀴즈 풀기"를 선택하면 객관식 2~3문항이 표시된다.
4. 퀴즈 문항에 모두 답하면 "문제 더 풀기" 버튼이 나타나고, 클릭 시 기존 문항과 겹치지 않는 새 문항이 추가된다.
5. 5번 항목에 정의된 10가지 예외 상황 각각에 대해, 지정된 사용자 문구가 실제로 노출되고 시스템이 명시된 동작대로 화면 상태를 유지/전환한다.
6. 로그인, 회원가입, 결제, DB(영속 저장소) 관련 코드나 UI가 존재하지 않는다.
7. 서로 다른 실제 강의자료 PDF 3종으로 테스트했을 때, 목차 인식·요약 밀도·퀴즈 난이도가 1)목표와 성공조건에 정의된 기준을 육안으로 충족한다.
8. 새로고침 시 모든 데이터(목차, 요약, 퀴즈)가 초기화되며, 이는 오류가 아닌 정상 동작으로 확인된다.
